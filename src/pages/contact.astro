---
import BaseLayout from '../layouts/BaseLayout.astro';
import { CONTACT, withBase } from '../config/site';
---

<BaseLayout
  title="Contact"
  description="Secure contact channels for vulnerability submissions, coordination inquiries, and PGP-encrypted communication."
  canonicalPath="/contact"
>
  <section class="max-w-4xl">
    <h1 class="font-display text-4xl font-bold">Contact</h1>
    <p class="mt-4 text-base leading-7 text-text-soft">
      Del Security uses static email channels only. No web form, no third-party intake service.
    </p>
  </section>

  <section class="mt-10 grid gap-5">
    <article id="security" class="card p-6">
      <h2 class="font-display text-2xl font-semibold">Security / Submissions</h2>
      <p class="mt-3 text-sm text-text-soft">
        Email: <a class="text-brand underline" href={`mailto:${CONTACT.securityEmail}`}>{CONTACT.securityEmail}</a>
      </p>
      <p class="mt-2 text-sm text-text-soft">
        For sensitive reports, encrypt content with our <a class="text-brand underline" href={withBase('/pgp.txt')}>PGP public key</a>.
      </p>
    </article>

    <article id="business" class="card p-6">
      <h2 class="font-display text-2xl font-semibold">Stakeholder Inquiries</h2>
      <p class="mt-3 text-sm text-text-soft">
        Email: <a class="text-brand underline" href={`mailto:${CONTACT.infoEmail}`}>{CONTACT.infoEmail}</a>
      </p>
      <p class="mt-2 text-sm text-text-soft">
        For assessment requests, coordination discussions, or retainer conversations.
      </p>
    </article>
  </section>
</BaseLayout>
