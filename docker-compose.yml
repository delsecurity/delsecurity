services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: del-security-site:dev
    ports:
      - "4321:3000"
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0 --port 3000
