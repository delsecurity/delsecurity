services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    ports:
      - "8080:80"
    environment:
      - SITE_URL=http://localhost:8080
    restart: unless-stopped
